# Canonical LU NIF Viewer

`LU NIF Viewer.exe` is generated here by `Run-NIF-Viewer.ps1` in the repository
root. It is the canonical executable for visual validation.

The generated executable is intentionally not committed. Run the launcher to
build or refresh it. Debug executables elsewhere under `build` are developer
artifacts and must not be used as visual references.
